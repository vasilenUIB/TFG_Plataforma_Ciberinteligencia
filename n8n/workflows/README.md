Exporta aquí los dos workflows desde la interfaz de n8n (menú del workflow → Download):

- `phishing.json` — workflow de detección de phishing por correo (sección 4.5.1 de la memoria).
- `reverse-shell.json` — workflow de detección de reverse shell vía Falco (sección 4.5.2 de la memoria).

Antes de exportar, revisa que no queden credenciales de Gmail ni otros nodos guardadas en el JSON exportado.
